# Third-party compiled bundle (install `oqs` via pip instead for native PQC)
ThreatX/liboqs/

# TLS certificates (private key) - regenerate via openssl (see README)
ThreatX/certs/

# Python artifacts
__pycache__/
*.pyc
*.pyo

# Runtime keys / editor config
ThreatX/session_key.bin
.vscode/